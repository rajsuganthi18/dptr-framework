{
  "name": "dptr-framework",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "build": "tsc",
    "experiment": "npm run build && node dist/src/experiments/experiment-runner.js",
    "analyze": "npm run build && node dist/src/experiments/analyze-results.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^4.9.5",
    "@types/node": "^18.0.0",
    "pixelmatch": "^5.3.0",
    "pngjs": "^6.0.0"
  }
}
